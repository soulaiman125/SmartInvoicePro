# Frontend production environment (Vercel → Project → Settings → Environment Variables)
#
# Absolute base URL of the deployed backend API (Railway/Render), including /api/v1.
# Example: https://smartinvoice-api.up.railway.app/api/v1
VITE_API_URL=https://your-api-host/api/v1
